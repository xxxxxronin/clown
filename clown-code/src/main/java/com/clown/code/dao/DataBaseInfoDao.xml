<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.clown.code.dao.DataBaseInfoDao">

    <!--*****************************************-->
    <!--获得数据库所有字段-->
    <!--*****************************************-->
    <select id="findAllTableNames" resultType="com.clown.code.model.TableInfoModel">
        select `table_name` as tableName,TABLE_COMMENT as tableComment from information_schema.tables where table_schema=#{dbName} and table_type='base table';
    </select>

    <!--*************************************-->
    <!--获得表的字段信息-->
    <!--*************************************-->
    <select id="findColumnInfo" resultType="com.clown.code.model.ColumnInfoModel">
        select column_name as columnName,DATA_TYPE as `dataType`,COLUMN_COMMENT as columnComment from information_schema.columns where table_schema=#{dbName} and table_name=#{tableName};
    </select>

</mapper>